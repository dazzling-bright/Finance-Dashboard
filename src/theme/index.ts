import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    brand: {
      50: "#f5f7ff",
      100: "#ebf0ff",
      500: "#1a365d",
      600: "#153e75",
      900: "#1a202c",
    },
  },
});

export default theme;
